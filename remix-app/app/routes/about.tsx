// We can't cmobile export default with const directly, it is an invalid javascript syntax
const About = () => {
    return (
        <div>
            <h1>About us root</h1>
        </div>
    )
}
export default About;